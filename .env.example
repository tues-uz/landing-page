# For production, set this to your backend's API URL (e.g., https://api.example.com/api/v1)
# For local development, you can leave this unset to use the Vite proxy (/api)

VITE_API_BASE_URL=

# Set to "true" to use the CMS without login (no /auth/me or /auth/login required). For local dev only.
# VITE_CMS_SKIP_AUTH=true
